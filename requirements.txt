# CAGE EMPIRE - Python dependencies
# Install with:  pip install -r requirements.txt
#
# Core (required): Python standard library only (sqlite3, tkinter,
# pathlib, datetime, random). No third-party packages required for
# the minimal dev build + test.
#
# GUI (Stage 6+): CustomTkinter + Pillow + ttkbootstrap for the
# dual-mode (Office + Fight Night) desktop GUI. Install these before
# running the new CTk-based UI (Task 6.1+).

# --- GUI framework (Stage 6 — Task 6.1) ---
customtkinter>=5.2.0      # polished dark UI widgets (replaces ttk)
pillow>=10.0.0            # portrait / image handling / heatmap compositing
ttkbootstrap>=1.10        # Treeview theming only (matches CTk dark theme)

# --- Optional (enable when those features land) ---
# rich>=13.0.0             # terminal pretty-print for debug / CLI mode
# pyyaml>=6.0              # mod / template loading
# openpyxl>=3.1            # CSV/XLSX mod import-export

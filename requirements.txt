# CAGE EMPIRE — Python dependencies
# Install with:  pip install -r requirements.txt
#
# The active UI is pywebview (desktop web app wrapper). The old
# CustomTkinter UI was removed in commit c7220d7.

# --- Required for the desktop UI ---
pywebview>=6.0            # desktop web app wrapper (the active UI)

# --- Python standard library (no install needed) ---
# sqlite3, pathlib, datetime, random, json, os, sys, etc.
